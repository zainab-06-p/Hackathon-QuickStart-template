// src/components/SpaceDesign/BackgroundParticles.tsx
// A placeholder wrapper to simplify imports if we want to swap implementations
import { StarBackground } from './StarBackground';

export const BackgroundParticles = StarBackground;
